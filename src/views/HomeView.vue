<template>
  <main class="home-page">
    <section class="hero-grid">
      <div class="hero-copy">
        <p class="hero-kicker">Hello World, I'm</p>
        <h1>Rio Adriana</h1>
        <div class="hero-role">{{ txt }}<span class="typing-caret"></span></div>
        <p class="hero-description">Welcome to my personal website. I build functional, easy-to-use websites with a personal touch.</p>
        <div class="hero-actions"><router-link to="/portfolio" class="primary-button">See my work <svg class="arrow-icon" viewBox="0 0 16 16" aria-hidden="true"><path d="M3 13 13 3M6 3h7v7" /></svg></router-link><a href="/cv/CV_RioAdriana_Eng.pdf" download="Resume_Rio_Adriana.pdf" class="secondary-button"><svg class="arrow-icon" viewBox="0 0 16 16" aria-hidden="true"><path d="M8 3v10M4.5 9.5 8 13l3.5-3.5" /></svg> Download Resume</a></div>
        <div class="hero-meta"><span>Based in Bandung, Indonesia · Available for collaboration</span></div>
      </div>
      <div class="hero-visual">
        <div class="profile-card glass-panel"><div class="card-topline"><span>RioAdriana();</span></div><div class="portrait-wrap"><img src="/img/yoojin.jpeg" alt="Rio Adriana" class="portrait" /></div><div class="profile-bottom"><div><strong>Web developer</strong><small>full-stack &amp; curious</small></div><span class="arrow-circle"><svg class="arrow-icon" viewBox="0 0 16 16" aria-hidden="true"><path d="M3 13 13 3M6 3h7v7" /></svg></span></div></div>
      </div>
    </section>
    <section class="home-note glass-panel"><p>“Programming is not only a job, but also a creative craft.”</p></section>
  </main>
</template>

<script>
export default { name: 'HomeView', data() { return { toRotate: ['Web Developer', 'Full-stack Engineer', 'Informatics Student'], period: 2000, txt: '', loopNum: 0, isDeleting: false, typewriterTimer: null }; }, mounted() { this.tick(); }, beforeUnmount() { clearTimeout(this.typewriterTimer); }, methods: { tick() { const word = this.toRotate[this.loopNum % this.toRotate.length]; this.txt = this.isDeleting ? word.substring(0, this.txt.length - 1) : word.substring(0, this.txt.length + 1); let delta = 200 - Math.random() * 100; if (this.isDeleting) delta /= 2; if (!this.isDeleting && this.txt === word) { delta = this.period; this.isDeleting = true; } else if (this.isDeleting && this.txt === '') { this.isDeleting = false; this.loopNum += 1; delta = 500; } this.typewriterTimer = setTimeout(() => this.tick(), delta); } } };
</script>

<style scoped>
.home-page { padding: 2rem 0; }.hero-grid { display: grid; grid-template-columns: 1.1fr .9fr; align-items: center; gap: 3rem; min-height: 560px; }.hero-copy { padding-left: clamp(0px, 4vw, 50px); text-align: left; }.hero-kicker { margin: 0 0 .7rem; color: var(--accent); font-size: 1rem; }.hero-copy h1 { margin: 0; color: var(--text); font-size: clamp(3rem, 6vw, 5.7rem); line-height: 1.03; letter-spacing: -.07em; }.hero-role { display: flex; align-items: center; min-height: 38px; margin-top: 1rem; color: var(--muted-strong); font-size: clamp(1.05rem, 2vw, 1.35rem); font-weight: 600; }.typing-caret { width: 2px; height: 1.15em; margin-left: .3rem; background: var(--accent); animation: blink 1s steps(1) infinite; }.hero-description { max-width: 430px; margin: 1.2rem 0 1.8rem; color: var(--muted); font-size: .92rem; line-height: 1.8; }.hero-actions { display: flex; flex-wrap: wrap; gap: .7rem; }.primary-button, .secondary-button { display: inline-flex; align-items: center; gap: .55rem; min-height: 45px; padding: .7rem 1rem; border-radius: 13px; font-size: .78rem; font-weight: 600; transition: transform .3s ease, background .3s ease, box-shadow .3s ease; }.primary-button { color: var(--accent-ink); background: var(--accent); box-shadow: 0 8px 20px rgba(255,219,112,.12); }.secondary-button { border: 1px solid var(--line-strong); color: var(--muted-strong); background: var(--surface); }.primary-button:hover, .secondary-button:hover { transform: translateY(-2px); box-shadow: 0 12px 22px rgba(0,0,0,.14); }.primary-button span, .secondary-button span { color: inherit; font-size: 1rem; }.hero-meta { display: flex; align-items: center; gap: .6rem; margin-top: 2.8rem; color: var(--muted); font-size: .65rem; }.meta-line { width: 24px; height: 1px; background: var(--line-strong); }.hero-visual { display: grid; place-items: center; min-height: 500px; }.profile-card { position: relative; z-index: 2; width: min(310px, 70vw); padding: .8rem; border-radius: 25px; transform: rotate(3deg); transition: transform .5s ease, box-shadow .5s ease; }.profile-card:hover { transform: rotate(0) translateY(-6px); box-shadow: 0 24px 50px rgba(0,0,0,.25); }.card-topline, .profile-bottom { display: flex; align-items: center; justify-content: space-between; }.card-topline { padding: .25rem .35rem .75rem; color: var(--muted); font-size: .64rem; }.card-spark { color: var(--accent); }.portrait-wrap { overflow: hidden; aspect-ratio: .86; border-radius: 19px; background: var(--surface-strong); }.portrait { display: block; width: 100%; height: 100%; object-fit: cover; filter: saturate(.82); }.profile-bottom { padding: .85rem .35rem .2rem; text-align: left; }.profile-bottom strong, .profile-bottom small { display: block; }.profile-bottom strong { color: var(--text); font-size: .85rem; }.profile-bottom small { margin-top: .2rem; color: var(--muted); font-size: .65rem; }.arrow-circle { display: grid; place-items: center; width: 30px; height: 30px; border: 1px solid var(--line); border-radius: 50%; color: var(--accent); }.home-note { display: flex; align-items: center; gap: .8rem; margin: 1rem auto 0; padding: .9rem 1.1rem; border-radius: 16px; color: var(--muted); font-size: .72rem; }.home-note p { margin: 0; color: var(--muted-strong); font-style: italic; }.note-mark { color: var(--accent); }.note-author { margin-left: auto; white-space: nowrap; color: var(--accent); font-size: .65rem; } @keyframes blink { 50% { opacity: 0; } }
@media (max-width: 760px) { .home-page { padding-top: .5rem; }.hero-grid { grid-template-columns: 1fr; gap: 1rem; min-height: auto; }.hero-copy { padding-left: 0; }.hero-visual { min-height: 410px; }.profile-card { width: min(275px, 74vw); }.hero-meta { margin-top: 2rem; }.home-note { align-items: start; font-size: .65rem; }.note-author { align-self: end; } }
@media (max-width: 420px) { .home-copy h1 { font-size: 3.25rem; }.home-note { display: none; } }
.arrow-icon { display: inline-block; width: .9em; height: .9em; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; flex: 0 0 auto; }
</style>
